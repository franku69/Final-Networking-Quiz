/* Packet Quest public GitHub Pages config.
   No teacher key, answer key, or private student records belong here.
   apiBase may remain blank: the teacher dashboard can pair this Pages site
   to a backend and shared room links carry the backend URL automatically. */
window.PACKET_CONFIG = {
  version: '7.1.0-stable',
  apiBase: '',
  siteBase: '',
  bankId: 'cs111-topic4-pdf-30-v6',
  questionCount: 30,
  allowApiOverride: true
};
